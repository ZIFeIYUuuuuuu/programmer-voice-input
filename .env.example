# This app does not read API keys from .env by default.
# Configure your DashScope API key in the local Settings window.
#
# Keep this file as documentation only. Never commit a real API key.
DASHSCOPE_API_KEY=
